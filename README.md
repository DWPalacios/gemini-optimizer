# gemini-optimizer
Reduce Gemini prompt costs by converting chat history into tall 768×N images, while keeping system and the latest USER as text. The auto mode decides using real token counts when it’s beneficial.
